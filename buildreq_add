# This file contains additional build requirements that did not get
# picked up automatically. One name per line, no whitespace.
gfortran
# To fix configure's static linkage detection at build time
glibc-staticdev
